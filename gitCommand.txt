此文件用于记录和回顾git相关的命令


####git安装和配置相关指令：

scoop install git

scoop updata git 

git config --global user.name "用户名"
git config --global user.email "用户电子邮件"

####本地仓库管理相关指令：

git init

git add 文件名

git commit -m "本次提交的说明"

git status

git diff xxxx
git diff HEAD -- 文件名

git log
git log --pretty=oneline

git reflog

git reset --hard HEAD^
git reset --hard xxxx

git chekout -- xxxx
git reset HEAD xxxx

git rm xxxx
git commit -m "xxxx"

git chekout -- xxxx


####远程仓库交互

git remote add origin git@github.com:xxxx/xxxx.git

git push -u origin master

git push origin master

git pull origin xxxx

git branch --set-upstream-to=origin/xxx xxx

git remote -v

git remote rm origin

git clone git@github.com:xxxx/xxxx.git

git switch -c xxx

git chekout -b xxx origin/xxx

git branch

git switch xxxx

git merge xxx
git merge --on-ff -m "xxxx" xxx

git branch -d xxx
git beanch -D xxx

git log --graph --pretty=oneline --abbrev-commit

git stash

git stash list

git stash pop

git stash apply stash@{x}
git stash drop

git cherry-pick xxxx

git rebase

git tag xxx
git tag -a xxx -m "xxxx" xxxx

git tag

git tag xxx xxxx

git show xxx

git tag -d xxx

git push origin xxx

git push origin --tags

git push origin :refs/tags/xxx







